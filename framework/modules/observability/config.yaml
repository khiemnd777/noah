server:
  host: "${M_OBSERVABILITY_HOST}"
  port: ${M_OBSERVABILITY_PORT}
  route: "/api/observability"

external: true

database:
  provider: "${DB_PROVIDER}"
  automigrate: ${DB_AUTOMIGRATE}
  postgres:
    host: "${PG_HOST}"
    port: ${PG_PORT}
    user: "${PG_USER}"
    password: "${PG_PASSWORD}"
    name: "${PG_DBNAME}"
    sslmode: "${PG_SSL}"

# Shared environment variables for the monorepo.
# Keep only cross-app values here. App-specific vars stay in /api/.env* and /fe/.env*.
# APP_* is reserved for shared values and should not be redefined in app-local env files.

APP_FE_ORIGIN=http://localhost:5173
